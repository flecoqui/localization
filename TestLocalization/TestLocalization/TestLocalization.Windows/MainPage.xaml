<Page
    x:Class="TestLocalization.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:TestLocalization"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Column="1" Grid.Row="0" Style="{StaticResource HeaderTextBlockStyle}"/>
        <TextBlock x:Uid="DisplayName" Grid.Column="1" Grid.Row="1" Style="{StaticResource HeaderTextBlockStyle}"/>
        <TextBlock x:Uid="Description" Grid.Column="1" Grid.Row="2" Style="{StaticResource HeaderTextBlockStyle}"/>
        <Image x:Uid="SourceForLogo" Grid.Column="1" Grid.Row="3"/>
    </Grid>
</Page>
